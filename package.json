{
  "name": "guiatv-monorepo",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "build": "npm run build --workspace apps/backend && npm run build:ssr --workspace apps/frontend",
    "build:frontend": "npm run build:ssr --workspace apps/frontend",
    "build:backend": "npm run build --workspace apps/backend",
    "start:api": "npm run start --workspace apps/backend",
    "start:ssr": "npm run serve:ssr --workspace apps/frontend",
    "db:bootstrap-local": "npm run create-indexes --workspace apps/backend && npm run job:syncEPG --workspace apps/backend && npm run job:precompute --workspace apps/backend",
    "deploy": "bash ./deploy-guiatv.sh"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
