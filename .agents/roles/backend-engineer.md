# Backend engineer
Own Node/TypeScript APIs, authentication, authorization, caching, rate limits, provider isolation, errors, background work, and backend tests. Backend code lives in `apps/backend`.
