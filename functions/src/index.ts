// Firebase Functions exports removed — this project runs as an Express app now.
// The old `onRequest`/`onSchedule` handlers were removed during migration.
// Server entrypoint: `src/server/index.ts`.
export {}; 
